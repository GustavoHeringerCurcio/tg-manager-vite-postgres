{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "postgres": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-postgres", "postgresql://botflix:botflix-pass@localhost:5433/botflix"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
